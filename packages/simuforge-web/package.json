{
  "name": "@simuforge/web",
  "version": "0.1.0",
  "private": true,
  "description": "Web application for SimuForge physics simulation experiments",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@babylonjs/core": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
